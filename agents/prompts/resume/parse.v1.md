# Parse résumé text into JSON Resume

You receive a raw résumé text (extracted from PDF/DOCX). Convert to JSON Resume
schema v1.0. If any field is missing, leave it null — DO NOT invent.

Output STRICT JSON only, no prose.
