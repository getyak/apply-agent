{
  "name": "@relay/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Relay browser extension — client-side delivery (MV3). See docs/architecture/client-side-delivery.md.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.279",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
