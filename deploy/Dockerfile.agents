FROM python:3.12-slim
WORKDIR /app
COPY agents ./agents
RUN pip install --no-cache-dir uv && cd agents && uv sync --no-dev
ENV PATH=/app/agents/.venv/bin:$PATH PYTHONPATH=/app
EXPOSE 8000
CMD ["uvicorn", "agents.api.server:app", "--host", "0.0.0.0", "--port", "8000"]
